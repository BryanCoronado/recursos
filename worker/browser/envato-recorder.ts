/** Compat Envato-only (usar automation-recorder con provider). */
export {
  closeAutomationRecorder,
  isRecorderBrowserOpen,
  isRecordingActive,
  openAutomationRecorder,
} from "./automation-recorder"
