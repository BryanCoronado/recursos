# Proxy Nginx para embeber noVNC en iframe (mismo dominio).
# Dentro del server { } de michitech.digital:
#
# location /vnc/ {
#     proxy_pass http://127.0.0.1:6080/;
#     proxy_http_version 1.1;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
#     proxy_set_header Host $host;
#     proxy_read_timeout 86400;
# }
#
# .env:
# NEXT_PUBLIC_NOVNC_URL="/vnc/vnc.html"
#
# La app añade sola: ?path=vnc/websockify&autoconnect=true
# Sin path=vnc/websockify el iframe queda negro (WS no conecta).
