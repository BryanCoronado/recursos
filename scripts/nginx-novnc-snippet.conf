# Ejemplo de proxy Nginx para embeber noVNC en iframe (mismo dominio = sin bloqueo).
# Colócalo dentro del server { } de michitech.digital
#
# location /vnc/ {
#     proxy_pass http://127.0.0.1:6080/;
#     proxy_http_version 1.1;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
#     proxy_set_header Host $host;
#     proxy_read_timeout 86400;
# }
#
# Luego en .env:
# NEXT_PUBLIC_NOVNC_URL="https://michitech.digital/vnc/vnc.html"
